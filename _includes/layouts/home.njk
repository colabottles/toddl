---
layout: layouts/base.njk
templateClass: tmpl-home
---

  <article class="about-intro">
    <div class="home-container">
      <h1 class="primary-heading">Hi, I'm Todd.</h1>
        <p>I'm a developer who is re-learning Java, Angular, and other tech and getting back to my development roots.</p>
      <h2 class="sub-heading">Web Developer / Developer Advocate</h2>
      <p>For hire. You can find more information on my <a href="https://toddl.dev/about/">About Me</a> page.</p>
    </div>
  </article>

  <div class="articles">
    <h3 class="list-label">Last Article</h3>
      <ul class="article-sections">
        <li class="article-wrapper">
          {%- for post in collections.bluesky | reverse -%}
          <a href="{{ post.url }}">
            <h4 class="article-link">{{ post.data.title }}</h4>
          </a>
          {%- endfor -%}
        </li>

        <li class="article-wrapper">
          {%- for post in collections.updated | reverse -%}
          <a href="{{ post.url }}">
            <h4 class="article-link">{{ post.data.title }}</h4>
          </a>
          {%- endfor -%}
        </li>

        <li class="article-wrapper">
          {%- for post in collections.update | reverse -%}
          <a href="{{ post.url }}">
            <h4 class="article-link">{{ post.data.title }}</h4>
          </a>
          {%- endfor -%}
        </li>
      </ul>
    </div>
